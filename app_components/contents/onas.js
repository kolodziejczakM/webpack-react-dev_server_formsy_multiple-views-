import React from 'react';
import ReactDOM from 'react-dom';

export default class Onas extends React.Component {
  render() {
    return (<div>We are company, which making websites. Just that.</div>
    	)
  }
}

ReactDOM.render(<Onas/>, document.getElementById('render'));
